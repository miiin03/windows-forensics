# 검증·무결성 구현 플랜 (windows-timeline)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development 또는 executing-plans. Steps use `- [ ]`.
> 본 프로젝트는 dev-harness panel1(claude)이 실행한다. 각 Task = harness 단계로 dispatch.

**Goal:** 루브릭 #3(건전성·검증)·#2(정확성)를 5점으로 올릴 windows-timeline 코드 작업 — 증거 해시/chain-of-custody, 재현성, carving 오탐 정량화, OSError 버그 수정, 무료도구 대조 메트릭.

**Architecture:** 기존 stdlib 엔진에 (a) `integrity.py`(해시/custody) 추가 + CLI 배선, (b) carving precision/recall 측정 하네스, (c) 대조 메트릭 스크립트, (d) OSError 버그 수정. 모두 read-only 유지, TDD.

**Tech Stack:** Python stdlib(`hashlib`, `json`), pytest. 외부 의존 0.

작업 트리: `windows-timeline/`. 기존 모듈(varint, record, carver/*, cli, ui_export 등) 재사용. 모든 테스트는 `cd windows-timeline && python -m pytest -q`.

---

## Task 1: 증거 해시 모듈 (integrity.py)

**Files:**
- Create: `windows-timeline/engine/integrity.py`
- Test: `windows-timeline/tests/test_integrity.py`

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_integrity.py`

```python
import hashlib
from engine.integrity import hash_file, custody_record

def test_hash_file_md5_sha256(tmp_path):
    p = tmp_path / "evidence.bin"
    data = b"ActivitiesCache sample bytes"
    p.write_bytes(data)
    h = hash_file(str(p))
    assert h["md5"] == hashlib.md5(data).hexdigest()
    assert h["sha256"] == hashlib.sha256(data).hexdigest()
    assert h["size"] == len(data)

def test_hash_file_missing_returns_none(tmp_path):
    assert hash_file(str(tmp_path / "nope.db")) is None

def test_custody_record_shape(tmp_path):
    p = tmp_path / "ActivitiesCache.db"; p.write_bytes(b"x" * 10)
    rec = custody_record(str(p), stage="acquired")
    assert rec["path"].endswith("ActivitiesCache.db")
    assert rec["stage"] == "acquired"
    assert rec["md5"] and rec["sha256"] and rec["size"] == 10
```

- [ ] **Step 2: 실패 확인** — `python -m pytest tests/test_integrity.py -q` → ModuleNotFoundError

- [ ] **Step 3: 구현** — `engine/integrity.py`

```python
"""Evidence integrity: hashing + chain-of-custody records (read-only)."""
from __future__ import annotations
import hashlib, os

_CHUNK = 1 << 20  # 1 MiB

def hash_file(path: str) -> dict | None:
    """Return {md5, sha256, size} streaming the file, or None if unreadable."""
    try:
        md5, sha = hashlib.md5(), hashlib.sha256()
        size = 0
        with open(path, "rb") as f:
            while True:
                chunk = f.read(_CHUNK)
                if not chunk:
                    break
                size += len(chunk); md5.update(chunk); sha.update(chunk)
        return {"md5": md5.hexdigest(), "sha256": sha.hexdigest(), "size": size}
    except OSError:
        return None

def custody_record(path: str, *, stage: str) -> dict:
    """One chain-of-custody entry for an evidence file at a stage."""
    h = hash_file(path) or {"md5": None, "sha256": None, "size": None}
    return {"path": path, "stage": stage, **h}
```

- [ ] **Step 4: 통과 확인** — `python -m pytest tests/test_integrity.py -q` → PASS
- [ ] **Step 5: 커밋** — `git add windows-timeline/engine/integrity.py windows-timeline/tests/test_integrity.py && git commit -m "feat(integrity): evidence MD5/SHA256 hashing + custody record"`

---

## Task 2: CLI에 해시·custody 배선 (수집 전/분석 후 무변경 증명)

**Files:**
- Modify: `windows-timeline/engine/cli.py` (분석 파이프라인 — 증거파일 목록 처리하는 곳)
- Modify: `windows-timeline/engine/ui_export.py` (meta에 evidence 해시 포함) 또는 export.py
- Test: `windows-timeline/tests/test_custody.py`

- [ ] **Step 1: 실패 테스트** — `tests/test_custody.py`

```python
import json
from engine.cli import build_parser, run

def test_output_meta_has_evidence_hashes(make_activity_db, tmp_path):
    gt = make_activity_db(n_insert=20, m_delete=5)
    out = tmp_path / "r.json"
    args = build_parser().parse_args([gt["db_path"], "--ui", "-o", str(out)])
    run(args)
    d = json.loads(out.read_text())
    ev = d["meta"]["evidence"]            # list of custody records
    assert any(e["path"].endswith("ActivitiesCache.db") for e in ev)
    e0 = ev[0]
    assert e0["md5"] and e0["sha256"]
    assert e0["stage"] in ("acquired", "verified")

def test_db_unmodified_after_run(make_activity_db, tmp_path):
    from engine.integrity import hash_file
    gt = make_activity_db(n_insert=20, m_delete=5)
    before = hash_file(gt["db_path"])
    out = tmp_path / "r.json"
    run(build_parser().parse_args([gt["db_path"], "--ui", "-o", str(out)]))
    after = hash_file(gt["db_path"])
    assert before == after            # read-only proven by hash equality
```

- [ ] **Step 2: 실패 확인** — `python -m pytest tests/test_custody.py -q` → KeyError 'evidence' / fail
- [ ] **Step 3: 구현** — cli 파이프라인에서 각 입력 db(+동반 -wal/-shm)와 evtx에 대해 `custody_record(path, stage="acquired")` 수집 전 1회, 분석 후 동일 파일 재해시해 `stage="verified"` 추가(또는 동일성 플래그). `meta["evidence"] = [records...]` 로 출력. ui_export.build_ui_output / export.build_output에 `evidence` 인자 추가해 meta에 실음. (정확한 코드는 cli.py 현재 구조 Read 후 작성 — custody_record 호출 + meta 주입.)
- [ ] **Step 4: 통과 확인** — `python -m pytest tests/test_custody.py -q` → PASS, 전체 회귀 green
- [ ] **Step 5: 커밋** — `git commit -m "feat(cli): chain-of-custody hashes in output meta, prove evidence unmodified"`

---

## Task 3: 재현성 테스트 (동일 입력 → 동일 출력)

**Files:**
- Test: `windows-timeline/tests/test_reproducibility.py`
- (필요시) Modify: 출력에서 비결정 필드(analyzed_at 등) 분리 가능하게 `ui_export`에 `analyzed_at` 주입 인자 활용

- [ ] **Step 1: 실패/검증 테스트**

```python
import json, hashlib
from engine.cli import build_parser, run

def _run(db, out):
    run(build_parser().parse_args([db, "--ui", "-o", out, "--analyzed-at", "2026-06-15T00:00:00Z"]))
    return json.loads(open(out).read())

def test_same_input_same_output(make_activity_db, tmp_path):
    gt = make_activity_db(n_insert=40, m_delete=12)
    a = _run(gt["db_path"], str(tmp_path/"a.json"))
    b = _run(gt["db_path"], str(tmp_path/"b.json"))
    # analyzed_at 고정 시 records/stats 결정적
    norm = lambda d: json.dumps({"stats": d["meta"]["stats"], "activities": d["activities"]}, sort_keys=True, ensure_ascii=False)
    assert hashlib.sha256(norm(a).encode()).hexdigest() == hashlib.sha256(norm(b).encode()).hexdigest()
```

- [ ] **Step 2: 실패 확인** — `--analyzed-at` 옵션 없으면 argparse error → 옵션 추가 필요
- [ ] **Step 3: 구현** — cli에 `--analyzed-at` 옵션 추가(없으면 now). build_ui_output(analyzed_at=...)로 전달(이미 인자 있음). records 정렬·dedup이 결정적인지 확인(GUID/page-offset 기준 정렬 보장).
- [ ] **Step 4: 통과 확인** — `python -m pytest tests/test_reproducibility.py -q` → PASS
- [ ] **Step 5: 커밋** — `git commit -m "test(repro): deterministic output with fixed analyzed_at; add --analyzed-at"`

---

## Task 4: carving 오탐 정량화 하네스 (precision/recall)

**Files:**
- Create: `windows-timeline/tests/test_carving_metrics.py`
- Create: `windows-timeline/bench/carving_metrics.py` (재사용 측정 함수 + CLI)

- [ ] **Step 1: 실패 테스트** — 합성 ground-truth로 precision/recall

```python
from bench.carving_metrics import measure

def test_carving_recall_high_precision_perfect(make_activity_db):
    # 200 insert, 60 delete (secure_delete OFF) → 삭제 GUID = ground truth
    gt = make_activity_db(n_insert=200, m_delete=60, payload_pad=200)
    m = measure(gt["db_path"], set(gt["deleted"]))
    assert m["recall"] >= 0.6          # 비할당에 남은 것 중 상당수 복원
    assert m["precision"] >= 0.95      # 복원된 GUID는 거의 다 실제 삭제분
    assert m["false_positives"] == 0   # clean fixture엔 유령 레코드 없음

def test_no_false_positives_on_clean_db(make_activity_db):
    gt = make_activity_db(n_insert=100, m_delete=0)   # 삭제 0
    m = measure(gt["db_path"], set())
    assert m["recovered"] == 0
```

- [ ] **Step 2: 실패 확인** — `python -m pytest tests/test_carving_metrics.py -q` → ModuleNotFoundError
- [ ] **Step 3: 구현** — `bench/carving_metrics.py`

```python
"""Measure carving precision/recall vs known-deleted GUID ground truth."""
from __future__ import annotations
import sys
from engine.pages import PageSource
from engine.carver.freelist import carve_freelist
from engine.carver.freeblock import carve_freeblocks

def _carved_guids(db_path: str) -> set[bytes]:
    ps = PageSource.from_file(db_path)
    cells = carve_freelist(ps) + carve_freeblocks(ps)
    return {c.values[0] for c in cells if c.values and isinstance(c.values[0], bytes)}

def measure(db_path: str, deleted_guids: set[bytes]) -> dict:
    recovered = _carved_guids(db_path)
    tp = len(recovered & deleted_guids)
    fp = len(recovered - deleted_guids)          # 삭제분 아닌 복원 = 오탐
    recall = tp / len(deleted_guids) if deleted_guids else 0.0
    precision = tp / len(recovered) if recovered else (1.0 if not deleted_guids else 0.0)
    return {"recovered": len(recovered), "true_positives": tp, "false_positives": fp,
            "recall": round(recall, 3), "precision": round(precision, 3)}

if __name__ == "__main__":   # python -m bench.carving_metrics <db>  (수동 점검)
    print(measure(sys.argv[1], set()))
```

(`bench/__init__.py` 빈 파일 생성, pyproject pythonpath에 포함되는지 확인.)

- [ ] **Step 4: 통과 확인** — `python -m pytest tests/test_carving_metrics.py -q` → PASS
- [ ] **Step 5: 실DB 측정 리포트** — 실 ActivitiesCache.db로 합성 삭제 시나리오 만들어 precision/recall 수치 기록(발표용). `git commit -m "feat(bench): carving precision/recall metrics harness"`

---

## Task 5: OSError 버그 수정 (실DB 일부 레코드)

**Files:**
- Investigate: `windows-timeline/engine/record.py`, `engine/overflow.py`, `engine/carver/*`
- Test: `windows-timeline/tests/test_overflow_robustness.py` (재현 후 작성)
- Modify: 진단된 파일

- [ ] **Step 1: 재현** — wintrace 통합 보고된 "일부 레코드 OSError". 실 ActivitiesCache.db로 `python -m engine <db> --ui -o /tmp/x.json` 돌려 OSError 트레이스 확보. 안 나면 overflow 큰 Payload/손상 WAL/큰 freelist 페이지 케이스를 합성해 재현.
- [ ] **Step 2: 진단** — 트레이스의 파일·라인 특정. 의심: overflow 체인 재조립 시 페이지 범위 밖 read(`pages.get_page` 음수/초과 page_no), 또는 큰 payload_len으로 slice 과다. 근본원인 1줄로 기술.
- [ ] **Step 3: 실패 테스트** — 재현 케이스를 결정적 테스트로 (예: overflow page_no가 db 범위 밖일 때 raise 대신 payload_truncated).

```python
def test_overflow_out_of_range_does_not_raise():
    # 조작된 cell: overflow page_no = 999999 (db 밖) → 부분복원, raise 금지
    ...
    cell = decode_table_leaf_cell(page, off, usable, overflow_reader=reader)
    assert cell.payload_truncated is True
```

- [ ] **Step 4: 수정** — bounds-check 추가(overflow page_no가 1..page_count 범위 아니면 중단·truncated). 모든 read OSError를 carver 경계에서 잡아 truncated 처리(절대 전파 금지).
- [ ] **Step 5: 통과 + 실DB 무에러 확인** — `python -m pytest -q` green + 실DB end-to-end OSError 0. `git commit -m "fix: bounds-check overflow chain, no OSError on real DB"`

---

## Task 6: 무료도구 대조 메트릭 스크립트 (자동 교차검증)

**Files:**
- Create: `windows-timeline/bench/compare_tools.py`
- Create: `windows-timeline/tests/test_compare_tools.py`

- [ ] **Step 1: 실패 테스트** — WxTCmd CSV(라이브) ↔ 우리 라이브 레코드수/Id 일치 검증 함수

```python
from bench.compare_tools import compare_live_with_wxtcmd_csv

def test_live_count_matches_wxtcmd(make_activity_db, tmp_path):
    gt = make_activity_db(n_insert=30, m_delete=0)
    # WxTCmd CSV를 모사: Id(GUID hex) 컬럼만 있는 CSV 생성
    csv = tmp_path / "wx.csv"
    import csv as c
    from engine.live_parser import read_activity
    ours = read_activity(gt["db_path"])
    with open(csv, "w", newline="") as f:
        w = c.writer(f); w.writerow(["Id"])
        for r in ours: w.writerow([r.values["Id"].hex()])
    res = compare_live_with_wxtcmd_csv(gt["db_path"], str(csv))
    assert res["match"] is True
    assert res["ours_count"] == res["wxtcmd_count"] == 30
```

- [ ] **Step 2: 실패 확인** — ModuleNotFoundError
- [ ] **Step 3: 구현** — `bench/compare_tools.py`: 우리 live GUID 집합 vs WxTCmd CSV의 Id 집합 비교 → {ours_count, wxtcmd_count, match, only_ours, only_wxtcmd}. carving 대조는 FQLite/undark 출력 파서 추가(수동 실행은 서브에이전트 MD `docs/benchmark/대조실험-가이드.md` 참조).
- [ ] **Step 4: 통과 확인** — PASS
- [ ] **Step 5: 커밋** — `git commit -m "feat(bench): auto cross-check live parse vs WxTCmd CSV"`

---

## Self-Review (spec 대비)
- D2.1(OSError) → Task 5 ✅
- D3.1(해시/custody) → Task 1+2 ✅
- D3.2(재현성) → Task 3 ✅
- D3.3(무료도구 대조) → Task 6(자동) + 서브에이전트 MD(수동 runbook) ✅
- D3.4(오탐 정량화) → Task 4 ✅
- D1/D4/D5(문서·발표·기여) = 코드 아님, 별도 문서 작업(이 플랜 밖)
- placeholder: Task 2/5는 현행 cli.py·트레이스 Read 후 정확 코드 — 구조상 불가피(투입 시 확정)
- 타입 일관: custody_record/hash_file/measure/compare_* 시그니처 Task 간 일치 ✅

## 실행
harness panel1로 Task 1→6 순차 dispatch. 각 Task = 1 단계, acceptance = 해당 테스트 green.
